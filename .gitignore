# Local/backend runtime files should not be published with the static Pages site.
backend/
.remote-staging/
.claude/

# Secrets and local databases.
.env
.env.*
*.db
*.sqlite
*.sqlite3
*credentials*.txt

# Python/runtime artifacts.
__pycache__/
*.py[cod]
.pytest_cache/

# OS/editor noise.
.DS_Store

# Node deps (never commit)
node_modules/

# Backend lives in its own repo (Makkie-Backend), not here.
lightsail-backend/
